# ui

Primitive UI components.

Implemented (Phase 9.3.6): `Button.tsx`, `Input.tsx`, `Textarea.tsx`, `Select.tsx`.
Not yet implemented: Toggle, Rating (no public page in this phase required a
Rating control from this list — the Client Review page's 1–5 scale uses
`Select` instead, see that route's `ClientReviewForm.tsx`).
