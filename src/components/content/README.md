# content

Content composites: Service Card, Project Card, Testimonial Card, Mono Chip.

Implemented (Phase 9.3.6): `ServiceCard.tsx`, `ProjectCard.tsx`,
`TestimonialCard.tsx`, `MonoChip.tsx` — all four now in use across the 9
public pages.
