# types

Reserved for shared TypeScript types per entity (Projects, Testimonials, Messages) once the
Prisma schema exists (Foundation Plan Stage 5 — Data Integration). Left empty at this
stage to avoid defining data shapes ahead of the schema itself.
